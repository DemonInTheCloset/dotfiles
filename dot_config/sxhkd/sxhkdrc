# Screenshooting
Print
    scrot "$HOME"/Pictures/'%F-%T_$wx$h'.png && \
        notify-send 'Took Screenshot' "~/Pictures/`date +'%Y-%m-%d'`_`xrandr | grep ' connected' | sed 's/\S* \S* \(\S*\)[+]\S*[+].*$/\1/; 1q'`.png"

# Media control
XF86Audio{Raise,Lower}Volume
    pamixer --{increase,decrease} 5 && audio-notify.sh

XF86AudioMute
    pamixer --toggle-mute && audio-notify.sh

# Brightness Control
XF86MonBrightness{Up,Down}
    xbacklight -perceived -{inc,dec} 5 && brightness-notify.sh

# Dmenu/Rofi
super + d
    rofi -modi drun,run -show drun

super + p
    passmenu

# Launch Programs
super + Return
    kitty

super + F2
    "$BROWSER"

super + shift + F2
    qutebrowser --target private-window

super + F3
    ranger

super + shift + v
    kitty -- mpv `xclip -o -selection clipboard`

# Shutdown
super + shift + s
    dmenu-prompt.sh 'Shutdown computer?' 'i3exit shutdown'

super + shift + p
    dmenu-prompt.sh 'Restart computer?' 'i3exit reboot'
